#!/usr/bin/pinafore

do
    Debug.message "A";
    drawChart <- Chart.getDraw;
    Debug.message "B";
    UI.openWindow (1000,400) {"Chart"} {[]} $ UI.draw $ {drawChart Chart.test};
    Debug.message "C";
end
