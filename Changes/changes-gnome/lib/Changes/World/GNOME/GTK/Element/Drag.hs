module Changes.World.GNOME.GTK.Element.Drag
    (
    ) where
{-
import Shapes
import GI.Gtk;
import Changes.Core
import Changes.World.GNOME.GTK.Element.GView

uiDragSource :: Serialize t => String -> Model (WholeUpdate t) -> Widget -> GView ()

uiDragDestination :: Serialize t => String -> Model (WholeUpdate t) -> Widget -> GView ()
-}
