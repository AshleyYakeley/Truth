#!/usr/bin/pinafore
let
opentype A;
opentype B;

datatype R of
    MkR of
    f: A -> Action Unit;
    end;
end;

r: R = let f = fn _ => outputLn.Env "Done." in MkR;

in r >- fn MkR =>
    let
    subtype B <: A
    in f (error "" : B)
