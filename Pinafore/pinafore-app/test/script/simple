#!/usr/bin/pinafore
let

# Ontology
# Define a type of entity for people
opentype Person;

# Properties of Person
name = property @Person @Text !"example:name";

# One particular Person
me = openEntity @Person !"example:me";

# GTK
ui_person : Person -> GTK.Element;
ui_person p = GTK.textEntry $ name !$ {p};

window_person : GTK.Context -> Person -> Action GTK.Window;
window_person gtk p = GTK.openWindow gtk (300,400) {"Name"} (ui_person p);

in GTK.run $ \gtk => do
    window_person gtk me;
    window_person gtk me;
    end
