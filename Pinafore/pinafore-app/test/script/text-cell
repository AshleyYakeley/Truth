#!/usr/bin/pinafore
import "gnome" in
openLocal.Store of end >>= fn store =>
run.GTK $ fn gtk =>
let
c = cell.Store @Text !"cell" store;
in open.Window.GTK gtk (400,500) {"Test"} $ textEntry.Widget.GTK c
