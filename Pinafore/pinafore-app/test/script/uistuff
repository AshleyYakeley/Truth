#!/usr/bin/pinafore

GTK.run $ \gtk => let
import "pinafore-gnome";
import "uistuff/selection";
in do
    tref1 <- newMemTextModel;
    tref2 <- newMemTextModel;
    paneWindow gtk $ pane "Text" $ vertical [layoutGrow $ textArea tref1, layoutGrow $ textArea tref2];
    end
