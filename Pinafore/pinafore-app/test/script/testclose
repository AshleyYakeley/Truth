#!/usr/bin/pinafore
let import "pinafore-gnome" in
run $
\gtk =>
fixAction $
\window =>
let mbar = menuBar [menuAction "Close" (Just "Ctrl+W") {closeWindow window}] in
openWindow gtk (400,600) {"Pinafore"} $
vertical
[mbar, layoutGrow $ button {"Close"} {closeWindow window}]
