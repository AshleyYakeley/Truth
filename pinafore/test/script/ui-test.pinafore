let

emptywindow :: Action ();
emptywindow = do
    openWindow {"Empty"} {[]} uiBlank;
    return ();
    end;

newpoint :: Action ();
newpoint = do s <- newMemFiniteSet; newEntity s; return (); end;

in do
    openWindow {"Test"} {[]} $ uiButton {"Button"} {newpoint >> emptywindow};
    return ();
    end
