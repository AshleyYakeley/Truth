let

emptywindow :: Action ();
emptywindow = do
    openWindow {"Empty"} (\_ -> {[]}) uiBlank;
    return ();
    end;

newpoint :: Action ();
newpoint = do s <- newMemFiniteSet; newEntity s; return (); end;

in do
    openWindow {"Test"} (\_ -> {[]}) $ uiButton {"Button"} {newpoint >> emptywindow};
    return ();
    end
