let
pdate = property @() @Date !"calendar-test";
rdate = pdate !$ {()};
in do
    openWindow {"Test Calendar"} {[]} $ uiVertical [(uiCalendar rdate,False),(uiLabel {toText %rdate},True)];
    return ();
    end
