do
dref <- newMemRef;
tref <- newMemRef;
iref <- newMemRef;
openWindow {"Date"} {[]} $ uiVertical
    [
        (uiTextEntry tref, False),
        (uiTextEntry tref, False),
        (uiTextEntry $ interpretDateAsText dref, False),
        (uiTextEntry $ interpretDateAsText dref, False),
        (uiTextEntry $ interpretIntegerAsText iref, False),
        (uiTextEntry $ interpretIntegerAsText iref, False)
    ];
return ();
end
