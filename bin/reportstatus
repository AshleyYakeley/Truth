#!/bin/bash -e
if [ "$1" -eq "0" ]; then
    echo "Succeeded";
    pw-play --volume=0.2 /usr/share/sounds/freedesktop/stereo/complete.oga
else
    echo "Failed: $1";
    pw-play --volume=0.5 /usr/share/sounds/mate/default/alerts/bark.ogg
fi
exit $1
