#!/bin/bash -e
if [ -z "${BINPATH}" ]; then
    export BINPATH=`stack path --local-bin`
fi
stack install hindent
for f in `find -name '*.hs' -not -path '*.stack-work/*' | grep -xvf .hindent.ignore`
do $BINPATH/hindent $f
done
